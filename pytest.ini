[pytest]
# Ensures the project root is importable (so "from api.app import app"
# works) regardless of which directory `pytest` is invoked from.
pythonpath = .

# Only collect tests from tests/ -- avoids pytest accidentally trying to
# collect from notebooks/, tools/, etc.
testpaths = tests
